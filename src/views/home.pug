extends layout

block content
    div(class='main')
        div(class='upload-box')
            div(class='userInfo')
                h3 你的唯一标识符为 #{token}
            div(class='tip')
                h3 图像上传建议
                ul
                    li 图像尺寸大于300*300，因为小心奈她的印章就有256*256那么大。你也不希望心奈的图章把你的画面全部覆盖吧。
                    li 图像大小在20M之内。

            form(action='/download', method='post', enctype="multipart/form-data")
                input(
                    class='upload-button',
                    type='file',
                    name='background'
                )
                input(
                    type='submit',
                    value='让小心奈给你盖个锉'
                )